#######################################################################
# local.conf
# 只放“本机特有 / 场景特有”的覆盖项；在 hyprland.conf 里务必最后 source。
# 用法：填好后 `hyprctl reload`，再用 `hyprctl monitors` / `hyprctl devices` 验证。
#######################################################################

monitor = HDMI-A-1, 3840x2160@60, 0x0, 1.5
