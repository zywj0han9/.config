# tmux 主配置文件 (Colemak + Hyprland 兼容)
# 清理默认键绑定，交由模块化文件定义
unbind-key -a -T prefix
unbind-key -a -T root
unbind-key -a -T copy-mode
unbind-key -a -T copy-mode-vi

# 入口: 加载核心引导系统
source-file ~/.config/tmux/core/bootstrap.conf
